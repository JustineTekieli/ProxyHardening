*.t linguist-language=Text
# Ensure all text files have LF line endings in the repository
* text=auto eol=lf

# Ensure that certain file types that are text-based have LF endings
*.c text eol=lf
*.h text eol=lf

# Ensure that shell scripts have LF endings
*.sh text eol=lf

# Ensure that batch scripts for Windows environments retain CRLF
*.bat text eol=crlf